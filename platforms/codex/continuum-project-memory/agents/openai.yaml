interface:
  display_name: "Continuum"
  short_description: "Durable project memory and recovery flow"
  default_prompt: "Use $continuum-project-memory to init, create tasks, spawn subagents, checkpoint progress, or continue durable project memory under .agent-memory."
